[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "storage-postgres"
version = "0.3.0"
description = "PostgreSQL storage, conditional writes, and atomic operations for agent-core"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["agent-core>=0.3.0,<0.4.0", "sqlalchemy>=2.0"]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
