[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-openai"
version = "0.1.0"
description = "OpenAI reasoning adapter for domain-agent-framework"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["domain-agent-core>=0.1.0,<0.2.0", "openai>=1.0"]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
